galaxy_info:
  author: ndench
  description: Lightweight server that supports the AWS S3 API
  license: MIT
  min_ansible_version: 2.4

  platforms:
  - name: Ubuntu
    versions:
    - xenial
    - trusty

  galaxy_tags:
      - development
      - web
      - aws
      - s3
      - fakes3

dependencies:
    - { role: geerlingguy.ruby, become: true }
